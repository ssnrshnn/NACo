# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3
python-multipart>=0.0.9

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.29
asyncpg>=0.29.0        # PostgreSQL async driver (production default)
aiosqlite>=0.20.0      # SQLite async driver (dev / tests only)
alembic>=1.13.1

# ── Cache / Rate limiting / Sessions ──────────────────────────────────────────
redis>=5.0.0

# ── Auth / Crypto ─────────────────────────────────────────────────────────────
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
cryptography>=42.0.5
pyotp>=2.9.0           # TOTP/2FA for admin login
ldap3>=2.9.1           # LDAP / Active Directory authentication

# ── RADIUS protocol ───────────────────────────────────────────────────────────
pyrad>=2.4

# ── TACACS+ (pure-Python implementation bundled in this project) ──────────────
# (no external package needed — implemented in naco/tacacs/)

# ── Network / Profiling ───────────────────────────────────────────────────────
scapy>=2.5.0           # DHCP sniffer + ARP monitor
psutil>=5.9.8

# ── Captive-portal QR codes ──────────────────────────────────────────────────
qrcode>=7.4.2
pillow>=10.3.0         # qrcode image backend

# ── Pydantic / settings ───────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml>=6.0.1
click>=8.1.7
httpx>=0.27.0          # async HTTP (webhooks, log forwarding)
